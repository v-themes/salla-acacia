{#
| Variable          | Type    | Description                                                      |
|-------------------|---------|------------------------------------------------------------------|
| items             | array   | List of sliders                                                  |
| items[].url       | string  |                                                                  |
| items[].link_type | ?String | category,product,offers,page,external_link,brand                 |
| items[].image.url | string  |                                                                  |
| items[].image.alt | string  |                                                                  |
| position          | Int     | Sorting number start from zero                                   |
#}
<section class="s-block s-block--photos-slider container {{ is_repeated?'repeated-block':'' }}">
    <salla-slider 
      type="carousel" 
      class="home-slider photos-slider"
      centered 
      auto-play 
      pagination
      type="fullwidth" 
      id="photos-{{ position }}-slider"> 
      <div slot="items">
        {% for item in items %}
          <div class="swiper-slide">
              <a href="{{item.url}}">
                <img loading="eager" width="100%" height="100%" src="{{ item.image.url }}" alt="banner-slide" class="w-full object-contain rounded-md">
              </a>
          </div>
        {% endfor %}
      </div>
    </salla-slider>
</section> 
